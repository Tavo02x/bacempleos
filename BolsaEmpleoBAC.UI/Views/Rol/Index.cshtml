@{
    ViewBag.Title = "Index";
}

<div ng-controller="FiltrarRolController" ng-init="SeguridadJS();LoadGrid();">
    <div class="d-md-flex justify-content-between align-items-center mb-5">
        <h1 class="subtitle-18"><span class="mdi mdi-settings mdi-24px"></span>Mantenimiento de Roles <span id="totalGrid"></span></h1>
        <button type="button" class="btn" ng-click="New()">Nuevo Rol</button>
    </div>
    <div class="row Panel">
        <div class="col-lg-12">
            <div id="table"></div>
        </div>
    </div>
</div>

@section scripts{

}